machine:
  python:
    version: 3.5.0
  services:
    - postgresql

test:
  override:
    - ./lint.sh
    - py.test --cov=em2 tests/
    - pip install -e .
  post:
    - coverage combine
    - coverage html
    - mv htmlcov $CIRCLE_ARTIFACTS/
