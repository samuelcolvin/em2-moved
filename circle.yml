machine:
  python:
    version: 3.5.0
  services:
    - postgresql

test:
  override:
    - flake8 em2pg/ tests/
    - py.test --cov=em2pg
  post:
    - coverage combine
    - coverage html
    - mv htmlcov $CIRCLE_ARTIFACTS/
