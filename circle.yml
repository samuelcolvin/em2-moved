machine:
  python:
    version: 3.5.0
  services:
    - postgresql

test:
  override:
    - flake8 --max-line-length 120 em2/ tests/
    - py.test --cov=em2 tests/
    - pip install -e .
  post:
    - coverage combine
    - codecov
