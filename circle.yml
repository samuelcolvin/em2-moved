machine:
  python:
    version: 3.5.0

dependencies:
  override:
    - python setup.py install
    - pip install -r dev_requirements.txt

test:
  override:
    - flake8 --max-line-length 120 em2_tests/ internal_tests.py
    - py.test --cov=em2_tests internal_tests.py
  post:
    - codecov
