# em2 can be installed via setup.py however it has strict requirements and should never be expected to
# hang out with other packages with conflicting requirements. Therefore all requirements are strict
# they're kept in a separate file to keep things explicit and to work with pyup

aiodns==1.1.1
aioredis==0.3.0
async-timeout==1.2.0
arq==0.6
Cerberus==1.1
msgpack-python==0.4.8
pycrypto==2.6.1
pytz==2017.2

# optionally used in logging
raven==6.0.0

# aiohttp
aiohttp==2.0.7
cchardet==2.0.1
uvloop==0.8.0

# postgres
SQLAlchemy==1.1.9
aiopg==0.13.0
psycopg2==2.7.1
